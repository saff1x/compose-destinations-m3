apply plugin: "com.vanniktech.maven.publish"

def tag = "git describe --tags --abbrev=0".execute().text.trim()

group = "io.github.raamcosta.compose-destinations"
version = tag

mavenPublishing {
    // publishToMavenCentral("S01")   // keep if you still release from your laptop
    // signAllPublications()          // ① delete or guard this line
}
